[build]
builder = "NIXPACKS"
buildCommand = "corepack enable && corepack pnpm install --no-frozen-lockfile && corepack pnpm run build"

[deploy]
startCommand = "corepack pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
