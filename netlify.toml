[build]
  base = "/"
  command = "node nexus-premium-ui/scripts/copy-maplibre-worker.mjs && cd nexus-premium-ui && next build && rm -rf ../.next && cp -R .next ../.next"
  publish = ".next"

[build.environment]
  PNPM_FLAGS = "--no-frozen-lockfile"
